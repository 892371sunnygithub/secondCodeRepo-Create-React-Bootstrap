const InstallationData={
    head:"Installation",
    para:"The best way to consume React-Bootstrap is via the npm package which you can install with npm (or yarn if you prefer).If you plan on customizing the Bootstrap Sass files, or don't want to use a CDN for the stylesheet, it may be helpful to install vanilla Bootstrap as well.",
    link:"npm install react-bootstrap bootstrap"
}

export default InstallationData;